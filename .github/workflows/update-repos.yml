name: Update All Repos

on:
  workflow_dispatch:
  schedule:
    - cron: '0 6 * * 0'

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: List repos
        run: |
          echo "This workflow syncs shared configs to all repos"
          echo "Repos to update: machbb, chromiumbb, tinyscript, FlatFree, gemini-gtk"
